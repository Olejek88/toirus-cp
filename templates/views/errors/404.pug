extends ../../layouts/default

block content
	.container-sm: .pv-2
		h1!=errorTitle || 'Не найдено (404)'
		if 'object' == typeof errorMsg
			div(style='margin: 30px 0;').debug!= htmlStringify(errorMsg)
		else
			p.lead!= utils.textToHTML(errorMsg) || 'Страница не найдена. По возможности сообщите администратором о проблеме с указанием адреса этой страницы.'
		p: a(href='/').btn.btn-primary.btn-lg На главную
